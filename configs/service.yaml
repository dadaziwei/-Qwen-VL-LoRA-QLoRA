# FastAPI 服务默认配置；也可以通过 .env 或环境变量覆盖。
backend: transformers
model: Qwen/Qwen2.5-VL-3B-Instruct-AWQ
host: 0.0.0.0
port: 8000
max_new_tokens: 512
temperature: 0.7
top_p: 0.9
# openai 后端会把请求代理到 vLLM 的 OpenAI 兼容接口。
vllm_base_url: http://127.0.0.1:8001/v1
